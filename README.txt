Extract all files, then open index.html in your browser.
Use the language selector at the top to choose between 10 interface and country-name languages.
All flags are included for offline use. Your language is remembered when browser storage is available.
Arabic uses a right-to-left layout. Names are sorted in the selected language; Japanese uses character collation, not phonetic readings.
